name: Makefile CI

on:
  push:
    branches:
      - main
      - develop
      - feature/**

jobs:
  build:
    name: Build and Refactor
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout repository
      uses: actions/checkout@v4

    - name: ⚙️ Install uv
      uses: astral-sh/setup-uv@b75a909d3a2a4f7f3e2b1c6f8e3e8e3e3e3e8e3e

    - name: 🧰 Run `make setup`
      run: make setup

    - name: 🧹 Run `make refactor`
      run: make refactor

    - name: 📁 List files in workspace
      run: ls -la "${{ github.workspace }}"
